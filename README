requirements:
	Endpoints must have at least one method
	Methods don't necessarily have parameters
	
todo:
	add code to show/hide methods for an endpoint and parameters for a method
	update formatting to show the hierarchy (endpoint -> methods -> parameters)
	validation on fields
	get links to methods working
	
future:
	drag and drop tree nodes to reorder things?